<Page
    x:Class="EvilCorp.Identifier.AccessPoint.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EvilCorp.Identifier.AccessPoint"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="Auto" Height="Auto">

    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--<Grid.Background>
            <ImageBrush ImageSource="/Assets/unicorn-profile.png" Stretch="UniformToFill" />
        </Grid.Background>-->
        <Image Source="/Assets/unicorn-profile.png" Margin="10,10,10,10" Width="{Binding Path=Height, RelativeSource={RelativeSource Self}}" VerticalAlignment="Stretch" Grid.Row="0"  Grid.Column="0"/>
        <Image Source="{Binding ControlQrc}"  Margin="10,10,10,10" Width="{Binding Path=Height, RelativeSource={RelativeSource Self}}" VerticalAlignment="Stretch" Grid.Row="0"  Grid.Column="1" />
    </Grid>
</Page>
